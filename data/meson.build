dtd_files = ['accounts-application.dtd',
    'accounts-provider.dtd',
    'accounts-service.dtd',
    'accounts-service-type.dtd'
]

install_data(dtd_files,
    install_dir: join_paths(get_option('datadir'), 'xml', 'accounts', 'schema', 'dtd')
)
